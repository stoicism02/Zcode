# Logger

Shows every log level streaming into the stream surface, plus `util.format` placeholders, `Error` unwrapping, markdown-rendered bodies, and `console.*` interception via `logger.install()`.

Run with `bun demo/logger.ts`.

::: code-group
<<< @/../demo/logger.ts [demo/logger.ts]
:::
